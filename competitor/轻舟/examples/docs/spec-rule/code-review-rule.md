# code-review-rule：团队 CR 检查项

> 本文件**只在 code-review 阶段加载**，编码阶段不读，用户事后防范。
> 收录团队主动声明的「过闸时必查」项：本身不是故障，是团队品味/风格约束（命名、写法偏好、禁用 API 等）。

## 检查项

<!-- 团队在此声明，一条一行，含出处。示例（自填后删除本注释）：
- 禁止使用 == / != 做相等比较，一律 ===/!==。
- 禁止 NaN 直接比较，用 Number.isNaN()。
-->
