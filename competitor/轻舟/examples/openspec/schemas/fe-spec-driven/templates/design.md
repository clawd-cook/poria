## 1. 技术概览

<!-- 技术栈 / 架构决策 / 包位置 / 构建工具 -->

## 2. 组件设计

<!-- 全部帧 nodeId 映射表；无设计稿时写"无设计稿，基于 PRD 文字描述" -->

| 序号 | 状态名称 | nodeId | 分组 | 置信度 |
|------|----------|--------|------|--------|

设计稿基准宽度：<!-- 如 375px -->

### 2.0 组件树

<!-- 【人审主视图】ASCII 树表达组件嵌套 + 复用 + 坑点，一处看全结构。由 `relay-schema-gen.py tree`
     从 parentmap(拓扑) join 对照表(语义，schema 层 component-table.md)生成——评审只看本树即可，无需逐行审对照表。
     节点标签：`名称 (类型) nodeId:X [reuse:...] — 备注`（reuse/备注 有才带）。 -->

```
Page
├── ProductFloor (业务组件) nodeId:45:21
│   ├── 商品主图 (业务组件) nodeId:45:24 [reuse:@/components/Image]
│   └── skuName (业务组件) nodeId:45:38 — ⚠幻影副标题，合并勿拆
└── PlaceholderCard (骨架容器) nodeId:142:22
```

### 2.1 视觉结构→nodeId 对照表

对照表（机器锚点区，非人审内容）已落 `delivery/<task>/schema/component-table.md` —— 供脚本校验(verify-tree)/组件树 join/投影装配(emit-projections)消费。**评审看上方 §2.0 组件树即可**（reuse/备注已 join 进树）。

<!-- 对照表本体在 schema 层文件，正文不塞整表。文件内每个组件一份，按组件聚合、不按帧平铺：
     组件 heading `#### 组件：<name> (rootNodeId: X)`（emit-projections 枚举/投影装配的锚，格式勿改）+
     表 `| 层级 | 名称 | nodeId | reuse | 备注(定性) |`（只贴语义、禁抄色值/尺寸/坐标；不列 parentId、不列渲染方式）+
     可选「跨状态差异」小表 `| 态 | 差异元素 | nodeId | 简述 |`。
     元素层留表门槛：只收「进 §2.4 数据绑定 ∨ 有实现坑点备注 ∨ 是 §2.2 变体 targetId ∨ 需 export_image 切图」
     的元素；纯装饰折进父节点备注、不逐一成行（细则见 relay-component-analysis.md「识别元素层」）。 -->

### 2.2 组件变体矩阵

<!-- subagent 产出「三张变体分表」时必做，原样搬入三张分表；无变体节点时写「设计稿未提供变体参考」。
     三表首列均为 rootNodeId（机器抽取键，与 2.1 对照表 heading 的 rootNodeId 同源）。格式见 schema.yaml §2 说明。 -->

**数量布局表（层①）**

| rootNodeId | 组件 | countFrom | case(数量) | presentation | item尺寸 | overflow | peek | 代表帧 nodeId |
|-----------|------|-----------|-----------|--------------|----------|----------|------|--------------|

**状态变体表（层②）**

| rootNodeId | 组件 | stateDim | 态 | 改哪个元素(targetId) | 改什么 | 代表帧 nodeId |
|-----------|------|----------|----|--------------------|--------|--------------|

**内容渲染表（层③）**

| rootNodeId | 元素 | 数据种类 | segments/差异 | 代表帧 nodeId |
|-----------|------|----------|--------------|--------------|

### 2.3 变体完整性自检

<!-- subagent 对账结论：设计稿实有变体帧（分母） vs 三张分表覆盖（分子）；差集逐帧判真缺/scope-excluded。
     无变体节点时写「设计稿未提供变体参考，无需自检」。 -->

### 2.4 数据绑定

<!-- 仅列有动态数据绑定的条目；本质是「哪个组件/元素消费哪个字段」，待第 5 章接口解析后填充 -->

| 组件/元素 | 数据来源 | 字段路径 | 说明 |
|-----------|----------|----------|------|

### 2.5 组件接入

<!-- 复用/接入决策：从项目已有库 import 的组件，逐个记参数与事件；组件协议未确认的先向用户确认。 -->

| 组件 | 类型 | 结论 | Props/事件 |
|------|------|------|-----------|

### 2.6 组件抽象决策

<!-- 承接 propose「组件合并候选」上报的合并候选；处置流程见 schema.yaml design 章「组件合并候选处置」（Tier1 自动 / Tier2 人确认 / 统一逻辑模型五步 + 退回闸）。本节记：
     - **合并决策**：`合并后组件名 ← 来源实例列表`、判别字段（如 taskType）、结论（合并为一个组件+变体 / 逻辑不可统一→退回独立）；
     - **统一逻辑模型摘要**：统一状态枚举 + 统一 Props（来源独有字段可选）+ 来源/类型变体轴——明细落 §2.2/§2.4/§3/§4.1，此处只留摘要+指针。
     无合并候选则写「无组件合并候选」。 -->

| 合并后组件 ← 来源实例 | 判别字段 | 统一状态枚举 | 结论 |
|------|------|------|------|

## 3. 状态设计

<!-- 状态管理方案 + 状态机流转图（Mermaid / ASCII） -->

## 4. 业务规则

### 4.1 状态视图矩阵

<!-- 横轴=对照表组件层/元素层条目，纵轴=第3章定义的所有状态 -->

| 状态 | 组件A | 组件B | 元素C | ... |
|------|-------|-------|-------|-----|

<!-- 单元格标注：显示/隐藏/文案变化/样式变化/数据变化 -->

### 4.2 交互清单表

<!-- 行来源：锚定对照表（§2.0 树 / schema 层 component-table.md）全部元素，逐个按「有无交互 affordance（看上去可点/可滑/可输入）」判定；
     有可供性的元素必须各占一行、无一遗漏（完整性闸，防"有箭头/按钮却没接行为"的孤儿元素）。
     详规列：复杂行为（领券/跳详）填 §4.x 引用既有规则、不重写；简单行为（入口跳转）就地写全。
     目标/参数：跳转目标或入参；PRD 未定义的显式写「PRD 待定」，禁止留空或臆造。 -->

| 元素 | nodeId | 触发 | 行为 | 目标/参数 | 详规 |
|------|--------|------|------|-----------|------|

### 4.3 补充规则

<!-- 矩阵与交互清单表无法完整表达的业务逻辑：页面入参定义、复合显隐条件、过滤/排序算法等 -->
<!-- 只写判定规则，不贴完整实现 -->

## 5. 接口分析

<!-- 每个接口：名称、状态（已上线/开发中/待开发）、入参 schema、出参 schema、mock 策略（未上线接口） -->

### 5.1 接口名

- 状态：已上线 / 开发中 / 待开发
- Mock 策略：<!-- 未上线时填：local-route / local-mock / easymock-http -->

**入参**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|

**出参**

| 字段 | 类型 | 说明 |
|------|------|------|

## 6. 异常流设计

### 6.1 接口异常矩阵

| 接口名 | 错误码/条件 | 前端行为 | 降级态 |
|--------|------------|---------|--------|

### 6.2 网络异常策略

| 场景 | 检测方式 | 用户感知 | 恢复机制 |
|------|---------|---------|---------|

### 6.3 交互防护

| 场景 | 防护策略 | 实现方式 |
|------|---------|---------|

### 6.4 操作中断与状态恢复

| 场景 | 表现 | 恢复策略 |
|------|------|---------|

### 6.5 数据边界兜底

| 场景 | 前端处理 |
|------|---------|

## 7. 埋点方案

<!-- 有埋点文档时：逐项写入事件名、触发时机、携带参数 -->
<!-- 无埋点文档时："无埋点文档，本章跳过" -->

## 8. 性能策略

<!-- 首屏 / 渲染 / 资源 / 内存 -->

## 9. 前端监控

### 9.1 JS 错误监控

<!-- 全局兜底 + 关键路径主动上报 -->

### 9.2 接口监控

| 接口 | 成功率阈值 | 耗时 P99 阈值 | 告警级别 |
|------|-----------|-------------|---------|

### 9.3 资源加载监控

| 资源类型 | 失败处理 | 上报方式 |
|---------|---------|---------|

## 10. 兼容性约束

<!-- 环境 / 适配 / 桥接能力 -->

## 11. 本迭代排除项

<!-- 用户明确排除的功能（标注"本迭代不做"+ 原因）；无排除项时写"无" -->
